import { ElementMrezeToAdd } from './element-mreze-to-add';

describe('ElementMrezeToAdd', () => {
  it('should create an instance', () => {
    expect(new ElementMrezeToAdd()).toBeTruthy();
  });
});
