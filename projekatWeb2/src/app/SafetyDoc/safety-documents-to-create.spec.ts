import { SafetyDocumentsToCreate } from './safety-documents-to-create';

describe('SafetyDocumentsToCreate', () => {
  it('should create an instance', () => {
    expect(new SafetyDocumentsToCreate()).toBeTruthy();
  });
});
